import React from 'react';
import PropTypes from "prop-types";
import { Alert } from "react-bootstrap";

function PintAlert(props) {

  return(
    <Alert variant='light'>
      Alert!
    </Alert>
  );
}

export default PintAlert;